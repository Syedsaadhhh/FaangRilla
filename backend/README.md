# OpenDoor Relay Backend

Python 3.12 FastAPI backend providing deterministic recovery orchestration, state machine validation, in-memory/DynamoDB persistence, and tokenized provider gateway adapters.
